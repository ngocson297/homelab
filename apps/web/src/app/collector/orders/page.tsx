import { Suspense } from 'react';import { cookies } from 'next/headers';import { redirect } from 'next/navigation';import { CollectorOrderListView } from '@/components/collector-order-list';import { CollectorShell } from '@/components/collector-shell';import { getStaffSession } from '@/lib/staff-auth-server';export default async function Page(){const user=await getStaffSession((await cookies()).toString());if(!user)redirect('/collector/login');if(user.role!=='COLLECTOR')return <p>Không có quyền truy cập.</p>;return <CollectorShell name={user.fullName}><Suspense fallback={<p>Đang tải…</p>}><CollectorOrderListView/></Suspense></CollectorShell>}
